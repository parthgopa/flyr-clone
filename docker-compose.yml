version: "3.9"

services:
  flask_app:
    container_name: flask_backend
    build: ./backend
    ports:
      - "5000:5000"
    env_file:
      - backend/.env
    volumes:
      - ./backend/uploads:/app/uploads
    restart: always
